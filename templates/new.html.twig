{% extends "base.html.twig" %}

{% block title %}Index{% endblock %}
{% block head %}
    {{ parent() }}
    <style type="text/css">
        .important { color: #336699; }
    </style>
{% endblock %}
{% block body %}
    <h1>Index</h1>
    <ul>
        {% for test in tests %}
            <li>{{ test.name }}</li>
            <li>{{ test.email }}</li>
            <li>{{ test.birthday|date('d/m/Y') }}</li>
            <li>{{ test.city }} {{ test.postalCode }}, {{ test.country }} </li>
            <li>{{ test.money }} €</li>
            <li><a href="{{ path('edit', {'id' : test.id}) }}"> Editer </a></li>
            <li><a href="{{ path('delete', {'id' : test.id}) }}"> Supprimer </a></li>
        {% endfor %}
    </ul>
    <p class="important">
        Welcome on my awesome homepage.
    </p>



    {{ form_start(form) }}


    {{ form_end(form) }}

{% endblock %}